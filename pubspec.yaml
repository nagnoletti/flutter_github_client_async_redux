name: github_repos_app
description: Sample Flutter project using package async_redux.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  async_redux: 13.2.0
  quiver: 3.0.1+1
  dio: ^4.0.4
  flutter_easyrefresh: ^2.2.1
  flutter_inappwebview: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true